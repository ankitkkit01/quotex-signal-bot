from setuptools import setup, find_packages

setup(
    name='quotexpy',
    version='1.0.0',
    description='Quotex API custom integration module',
    author='Ankit Singh',
    author_email='ankit@example.com',
    packages=find_packages(),
    install_requires=[],
)
